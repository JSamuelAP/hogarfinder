include partials/head
header
  nav.navbar.navbar-light.bg-light
    .container.justify-content-center
      a.navbar-brand(href='/') Logo HogarFinder
main.mt-5
  .container.text-center
    h1.fw-bold.mb-5 Inicie sesión
    .row.justify-content-center
      .col-10.col-sm-8.col-lg-6.col-xl-4
        form#form-login(action='/login/' method='POST')
          .mb-4
            label.form-label(for='select-tipo-cuenta') Tipo de cuenta
            select#select-tipo-cuenta.form-select(name='tipoCuenta' required)
              option(selected value='cliente') Cliente
              option(value='negocio') Negocio
              option(value='administrador') Administrador
          .mb-4
            label.form-label(for='input-correo') Correo
            input#input-correo.form-control(type='email' name='email' pattern='[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$' required)
          .mb-4
            label.form-label(for='input-password')
              | Contraseña
            input#input-password.form-control(type='password' name='password' required)
          button.btn.btn-info.btn-lg.text-white.px-5(type='submit')
            i.bi.bi-box-arrow-in-right.me-2
            | Log In
          p.mt-5
            | ¿No tiene cuenta? cree una:&nbsp;
            a.link-info(href='signup') Sign up
include partials/footer
include partials/scripts